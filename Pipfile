[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "*"

[requires]
python_version = "3.9"

[scripts]
dev = "flask run"
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
